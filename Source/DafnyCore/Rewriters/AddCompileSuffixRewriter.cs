namespace Microsoft.Dafny; 

public class AddCompileSuffixRewriter : IRewriter {
  internal AddCompileSuffixRewriter(ErrorReporter reporter) : base(reporter)
  {
  }
  
  internal virtual void PreResolve(ModuleDefinition moduleDefinition) {
    if (moduleDefinition.)
  }
}